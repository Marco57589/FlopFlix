<nav class="navbar navbar-expand-lg fixed-top text-white">
    <a class="navbar-brand">
        <img id="logo" class="logo-flopy" src="/img/logo.png" alt="Logo" title="Ciao!, io sono flopy">
    </a>

    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <i class="material-icons navbar-toggler-icon text-white mt-1">menu</i>
    </button>

    <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
            <li class="nav-item">
                <a class="nav-link text-white" href="/">Home</a>
            </li>
            <% if (user) { %>
            <li class="nav-item">
                <a class="nav-link text-white" href="mylist">La mia lista</a>
            </li>
            <% } %>
            <li class="nav-item">
                <a class="nav-link text-white" href="browse-category">Sfoglia per categoria</a>
            </li>
            <% if (user && user.ruolo === 'admin') { %>
            <li class="nav-item">
                <a class="nav-link text-dashboard" href="/admin-dashboard">| Admin Dashboard |</a>
            </li>
            <% } %>
        </ul>

        <div class="d-flex align-items-center">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0 mr-2">
                <li class="nav-item">
                    <form class="form-inline my-2 my-lg-0 search-form" action="/search" method="GET">
                        <button class="btn search-btn text-white" type="button">
                            <i class="material-icons">search</i>
                        </button>
                        <input class="search-input" type="search" name="query" placeholder="Cerca" required>
                    </form>
                </li>
                <% if (user && user.ruolo === 'user') { %>
                <li class="nav-item">
                    <div class="dropdown">
                        <a class="btn btn-secondary dropdown-toggle text-white" href="#" role="button" id="dropdownMenuLink" data-bs-toggle="dropdown" aria-expanded="false">
                            <img class="avatar" src="img/avatar.png">
                        </a>
                        <div class="dropdown-menu dropdown-menu-right" aria-labelledby="dropdownMenuLink">
                            <a class="dropdown-item text-white" href="account">
                                <div class="dropdown-align">
                                    <i class="material-icons dropdown-icon">person</i><p>Account</p>
                                </div>
                            </a>
                            <hr class="dropdown-hr">
                            <a id="logout" class="dropdown-item text-white" href="logout">Esci da FlopFlix</a>
                        </div>
                    </div>
                </li>
                <% } else if (user && user.ruolo === 'admin') { %>
                <li class="nav-item">
                    <a class="nav-link text-white" href="account">Account</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link text-white" href="logout">Logout</a>
                </li>
                <% } else { %>
                <li class="nav-item">
                    <a class="nav-link text-white" href="login">Accedi</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link text-white" href="register-step-1">Registrati</a>
                </li>
                <% } %>
            </ul>
        </div>
    </div>
</nav>
